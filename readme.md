# A working example of akka-r2dbc persistence/projection plugin
Project has docker compose file running a postgres database and

flyway is configured to create schema. mvn flyway:migrate.

after thar run Main.